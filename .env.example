# Git Configuration (Shared)
GIT_REMOTE_URL=git@github.com:username/obsidian-vault.git
GIT_USER_NAME=sbSync
GIT_USER_EMAIL=bot@sbsync.local

# SSH Configuration (Shared)
SSH_KEY_HOST_PATH=/Users/username/.ssh/id_ed25519

# Server Configuration (Push from Personal PC)
SERVER_TARGET_DIR=/vault-server
SERVER_METRICS_PORT=8000
SERVER_DEBOUNCE_SECONDS=300
SERVER_USE_POLLING=true

# Client Configuration (Pull to Work PC)
CLIENT_TARGET_DIR=/vault-client
CLIENT_METRICS_PORT=8001
CLIENT_PULL_INTERVAL_MINUTES=5
